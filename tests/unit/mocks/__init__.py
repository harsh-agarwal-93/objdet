"""Mock modules for testing without heavy dependencies."""

from __future__ import annotations

# Note: These modules use relative imports when accessed via pytest
# The individual mock modules can be imported directly from this package
