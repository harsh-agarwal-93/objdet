# Environment variables example for Webapp
# Copy this file to .env in the webapp/ directory (or webapp/frontend/.env and webapp/backend/.env if running separately)
# Note: For Docker Compose, these values are typically passed via the main docker-compose.yml or its env file.
# For local development (e.g. running `npm run dev` or `python main.py`), use this file.

# =============================================================================
# Frontend Configuration (Vite)
# =============================================================================
# URL for the backend API
VITE_BACKEND_URL=http://localhost:8000

# URL for the MLflow UI (used for linking from the dashboard)
VITE_MLFLOW_URL=http://localhost:5000

# =============================================================================
# Backend Configuration (FastAPI + Celery)
# =============================================================================
# Celery / RabbitMQ
CELERY_BROKER_URL=amqp://guest:guest@localhost:5672//
CELERY_RESULT_BACKEND=rpc://

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=objdet

# API Settings
# API_TITLE="ObjDet WebApp API"
# API_VERSION="0.1.0"
# API_DOCS_URL="/api/docs"

# CORS Settings (JSON formatted list)
# ALLOWED_ORIGINS='["http://localhost:3000", "http://frontend:80"]'
